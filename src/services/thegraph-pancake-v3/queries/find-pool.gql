query FindPool($id: String) {
  pools(where: { id: $id }) {
    id
    feeTier
    feesUSD
    volumeUSD
    liquidity
    totalValueLockedUSD
    token0 {
      id
      symbol
      decimals
    }
    token1 {
      id
      symbol
      decimals
    }
    token0Price
    token1Price
  }
}
